export const selectors = {
	title: 'android=new UiSelector().text("Home")',
	homeBtn: 'android=new UiSelector().text("🏠")',
	mailBtn: 'android=new UiSelector().text("✉️")',
	settingsBtn: 'android=new UiSelector().text("⚙️")',
};
