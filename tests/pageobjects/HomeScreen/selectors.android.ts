export const selectors = {
	title: 'android=new UiSelector().text("Home")',
	homeBtn: 'android=new UiSelector().text("Home")',
	mailBtn: 'android=new UiSelector().text("Email")',
	settingsBtn: 'android=new UiSelector().text("Settings")',
};
